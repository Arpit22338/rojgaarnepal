"use client";
import TeacherLoginToggle from "./TeacherLoginToggle";

export default function TeacherLoginToggleClient({ initialValue }: { initialValue: boolean }) {
  return <TeacherLoginToggle initialValue={initialValue} />;
}
